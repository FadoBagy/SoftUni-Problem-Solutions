function solve(input, input2, input3) {
    let lengthSum = input.length + input2.length + input3.length;
    let avarageLength = lengthSum / 3;
    console.log(lengthSum);
    console.log(Math.round(avarageLength));
}